---
phase: 03-templates-preview-and-export
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/templates/modern-template.tsx
  - src/components/templates/minimal-template.tsx
  - src/components/templates/template-registry.ts
  - src/components/preview/template-switcher.tsx
  - src/components/preview/page-indicator.tsx
  - src/components/editor/editor-layout.tsx
  - src/app/page.tsx
  - src/__tests__/template-switcher.test.tsx
  - src/__tests__/preview-panel.test.tsx
  - src/__tests__/editor-layout.test.tsx
autonomous: true
requirements: [TMPL-01, TMPL-03, PREV-01]

must_haves:
  truths:
    - "Three distinct templates are visually different and selectable"
    - "Switching templates does not lose any entered resume data"
    - "Live preview updates in real time as user edits form fields"
    - "Desktop shows sidebar, editor, and preview simultaneously"
    - "Mobile shows edit/preview toggle tabs"
  artifacts:
    - path: "src/components/templates/modern-template.tsx"
      provides: "Modern two-column resume template"
      exports: ["ModernTemplate"]
    - path: "src/components/templates/minimal-template.tsx"
      provides: "Minimal typography-focused resume template"
      exports: ["MinimalTemplate"]
    - path: "src/components/preview/template-switcher.tsx"
      provides: "Template thumbnail selector UI"
      exports: ["TemplateSwitcher"]
    - path: "src/components/editor/editor-layout.tsx"
      provides: "Three-panel layout: sidebar | editor | preview"
  key_links:
    - from: "src/components/preview/template-switcher.tsx"
      to: "src/lib/store/resume-store.ts"
      via: "setSelectedTemplate action"
      pattern: "setSelectedTemplate"
    - from: "src/components/editor/editor-layout.tsx"
      to: "src/components/preview/preview-panel.tsx"
      via: "dynamic import with ssr: false"
      pattern: "dynamic.*preview-panel.*ssr.*false"
    - from: "src/components/preview/preview-panel.tsx"
      to: "src/lib/store/resume-store.ts"
      via: "useResumeStore subscription for real-time updates"
      pattern: "useResumeStore"
---

<objective>
Build Modern and Minimal templates, template switcher UI, and the split-panel live preview layout.

Purpose: Complete the template set (TMPL-01), enable template switching (TMPL-03), and integrate the live preview panel into the editor layout (PREV-01). After this plan, users can see their resume update in real time while editing.

Output: Modern template, Minimal template, template switcher with thumbnails, three-panel editor layout (desktop) with edit/preview toggle (mobile), page indicator.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-templates-preview-and-export/03-RESEARCH.md
@.planning/phases/03-templates-preview-and-export/03-01-SUMMARY.md

<interfaces>
<!-- Key types/contracts from Plan 01 that this plan depends on -->

From src/components/templates/template-registry.ts:
```typescript
export type TemplateId = 'classic' | 'modern' | 'minimal'
export interface TemplateDefinition {
  id: TemplateId
  name: string
  description: string
  component: React.FC<{ resume: ResumeData }>
  accentColor: string
}
export const TEMPLATES: Record<TemplateId, TemplateDefinition>
export const DEFAULT_TEMPLATE: TemplateId
```

From src/components/templates/shared/pdf-primitives.tsx:
```typescript
export function SectionTitle(props: { title: string; color?: string; style?: object }): JSX.Element
export function DateRange(props: { startDate?: string; endDate?: string }): JSX.Element
export function BulletList(props: { items: string[] }): JSX.Element
export function ContactLine(props: { basics: ContactInfo }): JSX.Element
```

From src/components/templates/fonts.ts:
```typescript
export const FONT_FAMILY: string  // 'Inter'
```

From src/lib/store/resume-store.ts:
```typescript
selectedTemplate: TemplateId
setSelectedTemplate: (id: TemplateId) => void
```

From src/components/preview/preview-panel.tsx:
```typescript
export default function PreviewPanel(): JSX.Element  // Must be dynamically imported with ssr: false
```

From src/components/editor/editor-layout.tsx (current):
```typescript
export function EditorLayout(): JSX.Element
// Currently: sidebar (hidden md:block w-64) | main content (flex-1)
// Mobile: fixed tabs at top + content below
```

From src/hooks/use-section-nav.ts:
```typescript
export function useSectionNav(): {
  activeSection, setActiveSection, visibleSections,
  availableOptionalSections, enableOptionalSection, ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Modern and Minimal templates, update registry</name>
  <files>
    src/components/templates/modern-template.tsx,
    src/components/templates/minimal-template.tsx,
    src/components/templates/template-registry.ts
  </files>
  <action>
1. Create `src/components/templates/modern-template.tsx`:
   - Import Document, Page, View, Text, StyleSheet from @react-pdf/renderer
   - Import shared primitives and fonts
   - Props: `{ resume: ResumeData }` — pure renderer, NO hooks, NO state
   - **Visual style per CONTEXT.md:** Bold header with accent color bar/background block + two-column layout
   - Accent color: #2B6CB0 (blue)
   - Layout: TWO-COLUMN using flexbox (flexDirection: 'row')
     - Left sidebar column (~30% width): Contact info, skills, languages, certifications
     - Main column (~70% width): Summary, experience, education, projects, volunteer
   - Header: Full-width bold section at top with name + title on colored background block
   - Distinct from Classic: more visual weight, color blocks, sidebar layout
   - Handle all optional sections: render only if array is defined and non-empty
   - Use Inter font, A4 page

2. Create `src/components/templates/minimal-template.tsx`:
   - Import Document, Page, View, Text, StyleSheet from @react-pdf/renderer
   - Import shared primitives and fonts
   - Props: `{ resume: ResumeData }` — pure renderer, NO hooks, NO state
   - **Visual style per CONTEXT.md:** Elegant understated. Thin hairline separators, small caps for section headers, tight typography
   - Accent color: #1A202C (near-black)
   - Layout: Single-column, tight spacing
   - Section headers: SMALL CAPS style (textTransform: 'uppercase', fontSize smaller, letterSpacing wider)
   - Separators: Thin hairline rules (View with borderBottom: 0.5pt solid #E2E8F0)
   - Typography: Tighter line height, smaller margins between items
   - Sophisticated restraint — not empty, not cluttered
   - Handle all optional sections
   - Use Inter font, A4 page

3. Update `src/components/templates/template-registry.ts`:
   - Import ModernTemplate and MinimalTemplate
   - Replace placeholder entries with real components
   - Verify all three entries have correct metadata:
     - classic: { accentColor: '#4A5568', name: 'Classic', description: 'Clean professional style' }
     - modern: { accentColor: '#2B6CB0', name: 'Modern', description: 'Bold two-column layout' }
     - minimal: { accentColor: '#1A202C', name: 'Minimal', description: 'Elegant typography' }
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    TypeScript compiles without errors for all three template files. Template registry has all three entries.
  </verify>
  <done>
    - Modern template renders two-column layout with sidebar and colored header block
    - Minimal template renders single-column with small caps headers and hairline separators
    - Both are pure renderers (no hooks, no state) accepting ResumeData prop
    - Template registry has all 3 templates with correct metadata
    - All three templates are visually distinct from each other
  </done>
</task>

<task type="auto">
  <name>Task 2: Build template switcher, page indicator, update editor layout to three-panel with preview</name>
  <files>
    src/components/preview/template-switcher.tsx,
    src/components/preview/page-indicator.tsx,
    src/components/editor/editor-layout.tsx,
    src/app/page.tsx,
    src/__tests__/template-switcher.test.tsx,
    src/__tests__/preview-panel.test.tsx,
    src/__tests__/editor-layout.test.tsx
  </files>
  <action>
1. Create `src/components/preview/template-switcher.tsx`:
   - 'use client' directive
   - Import TEMPLATES, TemplateId from template-registry
   - Import useResumeStore for selectedTemplate and setSelectedTemplate
   - Render thumbnail cards for each template:
     - Show template name and a small preview indicator (colored accent bar or badge)
     - Active template has highlighted border/ring
     - Click instantly switches template (no confirmation dialog per CONTEXT.md)
   - Use shadcn Button or Card components for consistent styling
   - Layout: horizontal row of template options, responsive

2. Create `src/components/preview/page-indicator.tsx`:
   - 'use client' directive
   - Simple component showing "Page X of Y" text
   - Props: `{ currentPage: number; totalPages: number }`
   - Initially shows "Page 1 of 1" — actual page count from PDF is hard to detect; use a simple version now
   - Style: small text below the preview panel, muted color

3. Modify `src/components/editor/editor-layout.tsx` for three-panel layout:
   - **Desktop (md and above):** Three-panel layout using flex
     - Left: Sidebar (existing, w-64, border-r, shrink-0)
     - Center: Editor forms (flex-1, min-w-0, scrollable)
     - Right: Preview panel (w-[400px] or similar, border-l, shrink-0, hidden on screens < lg, visible on lg+)
     - Preview panel loads via `dynamic(() => import('@/components/preview/preview-panel'), { ssr: false })` with a loading skeleton
     - Template switcher sits above the preview panel inside the right column
     - Page indicator below the preview
   - **Tablet (md to lg):** Sidebar + editor only. Preview accessible via a toggle button in header area.
   - **Mobile (below md):** Keep existing mobile tabs for section navigation. Add "Preview" as an additional tab option. When Preview tab is active, show full-screen preview with template switcher above it. When any other tab is active, show the editor form.
   - Ensure editor forms are still scrollable and max-width constrained
   - The preview panel subscribes to useResumeStore — any form edit triggers store update which triggers preview re-render (PREV-01: real-time updates)

4. Update `src/app/page.tsx` if needed — EditorLayout handles everything, but verify it still works as the app entry point.

5. Create `src/__tests__/template-switcher.test.tsx`:
   - Mock resume store (selectedTemplate, setSelectedTemplate)
   - Test that all three template options are rendered
   - Test that clicking a template calls setSelectedTemplate with correct ID
   - Test that active template is visually distinguished (has active class/variant)

6. Create/update `src/__tests__/preview-panel.test.tsx`:
   - Mock @react-pdf/renderer (PDFViewer) since it cannot render in jsdom
   - Test that PreviewPanel reads resume and selectedTemplate from store
   - Test that it passes resume data to the template component

7. Update `src/__tests__/editor-layout.test.tsx`:
   - Existing tests may need mocking of the dynamic preview panel import
   - Add test that the layout renders sidebar, editor, and preview sections (at desktop viewport)
   - Mock the preview panel dynamic import to avoid @react-pdf/renderer issues in jsdom
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/template-switcher.test.tsx src/__tests__/preview-panel.test.tsx src/__tests__/editor-layout.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - Template switcher shows all 3 templates and switches on click without data loss
    - Desktop layout shows sidebar | editor | preview simultaneously
    - Mobile layout toggles between edit and preview modes
    - Live preview updates in real time as user edits (PREV-01)
    - Template switcher placed above preview panel per CONTEXT.md
    - Page indicator shows below preview
    - All tests pass including updated editor-layout tests
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no TypeScript errors
- `npx vitest run --reporter=verbose` — all tests pass
- Three template files exist with distinct visual styles
- Template switcher renders all three options
- Editor layout has three panels on desktop
- Mobile has edit/preview toggle
- Switching templates preserves all resume data (store is unchanged, only template component swaps)
</verification>

<success_criteria>
- Three distinct, ATS-safe templates with different visual styles (TMPL-01)
- Templates are pure renderers — switching only changes which component renders, data untouched (TMPL-03)
- Live preview updates when any form field changes (PREV-01)
- Three-panel desktop layout: sidebar | editor | preview
- Mobile: edit/preview tab toggle
- Template switcher with thumbnail-style cards above preview
- All new and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-templates-preview-and-export/03-02-SUMMARY.md`
</output>
