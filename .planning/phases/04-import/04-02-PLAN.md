---
phase: 04-import
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/import/import-button.tsx
  - src/components/import/import-modal.tsx
  - src/components/import/dropzone.tsx
  - src/components/import/text-paste.tsx
  - src/components/import/review-panel.tsx
  - src/components/editor/editor-layout.tsx
  - src/__tests__/import-modal.test.tsx
autonomous: false
requirements: [IMPT-01, IMPT-02, IMPT-03]

must_haves:
  truths:
    - "Import button visible in header bar next to Export menu"
    - "Clicking Import opens a modal with drag-and-drop dropzone"
    - "Dropzone accepts .pdf, .txt, and .json files"
    - "5 MB file size limit enforced client-side with visible message"
    - "PDF upload sends file to /api/parse, displays loading spinner with status text"
    - "Plain text can be pasted in a textarea tab or uploaded as .txt file"
    - "JSON backup files validated against Zod schema and loaded directly"
    - "After parsing, review panel shows all sections with checkboxes"
    - "User can deselect sections they don't want imported"
    - "User can inline-edit fields in the review panel"
    - "Uncertain fields highlighted with yellow/amber styling"
    - "Existing data warning shown with Continue/Cancel and backup download link"
    - "Accepting import commits data to editor via setResume()"
    - "Success toast shown after import accepted"
  artifacts:
    - path: "src/components/import/import-button.tsx"
      provides: "Import button for header bar"
      exports: ["ImportButton"]
    - path: "src/components/import/import-modal.tsx"
      provides: "Modal container with upload/review states"
      exports: ["ImportModal"]
    - path: "src/components/import/dropzone.tsx"
      provides: "Drag-and-drop file upload area"
      exports: ["Dropzone"]
    - path: "src/components/import/text-paste.tsx"
      provides: "Plain text paste textarea"
      exports: ["TextPaste"]
    - path: "src/components/import/review-panel.tsx"
      provides: "Section review with checkboxes and inline editing"
      exports: ["ReviewPanel"]
  key_links:
    - from: "src/components/import/import-modal.tsx"
      to: "src/app/api/parse/route.ts"
      via: "fetch POST /api/parse for PDF upload"
      pattern: "fetch.*api/parse"
    - from: "src/components/import/import-modal.tsx"
      to: "src/lib/import/section-mapper.ts"
      via: "mapSections for text classification"
      pattern: "import.*mapSections"
    - from: "src/components/import/import-modal.tsx"
      to: "src/lib/store/resume-store.ts"
      via: "setResume to commit imported data"
      pattern: "setResume"
    - from: "src/components/editor/editor-layout.tsx"
      to: "src/components/import/import-button.tsx"
      via: "ImportButton added to header"
      pattern: "ImportButton"
---

<objective>
Build the import UI: dropzone, text paste, review panel with inline editing, and wire everything into the editor layout.

Purpose: Give users a complete import flow — upload PDF/text/JSON, review parsed sections with confidence indicators, edit and select what to import, then commit to the editor.
Output: Import button, modal with upload + review states, and editor layout integration.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-import/04-CONTEXT.md
@.planning/phases/04-import/04-RESEARCH.md
@.planning/phases/04-import/04-01-SUMMARY.md
@src/components/editor/editor-layout.tsx
@src/components/export/export-menu.tsx
@src/components/export/json-export.ts
@src/lib/store/resume-store.ts
@src/lib/schema/resume-schema.ts
@src/types/resume.ts
@src/lib/import/pdf-types.ts
@src/lib/import/section-mapper.ts
@src/lib/import/text-parser.ts

<interfaces>
<!-- Key types and contracts from Plan 01. -->

From src/lib/import/pdf-types.ts (created in Plan 01):
```typescript
export type SectionType = 'contact' | 'summary' | 'experience' | 'education' | 'skills' | 'certificates' | 'projects' | 'languages' | 'volunteer' | 'unknown'

export interface MappedSection {
  type: SectionType
  content: string
  confidence: 'high' | 'medium' | 'low'
  rawLines: string[]
}

export interface ParseResponse {
  text: string
  pages: number
}

export interface ParseError {
  error: string
  scanned?: boolean
}

export interface ImportResult {
  resume: Partial<ResumeData>
  sections: MappedSection[]
}
```

From src/lib/import/section-mapper.ts:
```typescript
export function mapSections(lines: string[]): MappedSection[]
```

From src/lib/import/text-parser.ts:
```typescript
export function parseText(raw: string): string[]
export function cleanLines(lines: string[]): string[]
```

From src/lib/store/resume-store.ts:
```typescript
interface ResumeStore {
  resume: ResumeData
  setResume: (data: ResumeData) => void
  // ... other methods
}
export const useResumeStore = create<ResumeStore>()(...)
```

From src/components/export/json-export.ts:
```typescript
export function exportJson(resume: ResumeData, selectedTemplate: string): void
```

From src/components/editor/editor-layout.tsx (header bar area, line 68-71):
```tsx
<header className="hidden md:flex items-center justify-between px-6 py-3 border-b bg-background shrink-0">
  <h1 className="text-lg font-semibold">CV Builder</h1>
  <ExportMenu />
</header>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dropzone, text paste, and review panel components</name>
  <files>src/components/import/dropzone.tsx, src/components/import/text-paste.tsx, src/components/import/review-panel.tsx</files>
  <action>
    1. Create `src/components/import/dropzone.tsx`:
       - Native HTML5 drag-and-drop (no react-dropzone dependency)
       - Props: `onFile: (file: File) => void`, `disabled?: boolean`
       - Accept attribute: `.pdf,.txt,.json`
       - Visual states: default (dashed border, upload icon), dragging (primary border, primary/5 bg), disabled (opacity-50)
       - Show "Max 5 MB" text below the main prompt
       - Show file type hints: "PDF, TXT, or JSON"
       - Use Upload icon from lucide-react
       - Hidden file input triggered by clicking the dropzone area
       - Client-side 5MB validation: check `file.size > 5 * 1024 * 1024` before calling onFile, show toast error if exceeded

    2. Create `src/components/import/text-paste.tsx`:
       - Props: `onText: (text: string) => void`, `disabled?: boolean`
       - Textarea for pasting plain text resume content
       - "Import Text" button below textarea (disabled when textarea empty)
       - Textarea uses min-h-[200px] with resize-y
       - Placeholder text: "Paste your resume text here..."

    3. Create `src/components/import/review-panel.tsx`:
       - Props: `sections: MappedSection[]`, `onAccept: (selectedSections: MappedSection[]) => void`, `onCancel: () => void`
       - Each section displayed as a Card with:
         - Checkbox (checked by default) to include/exclude section
         - Section type label (capitalized) and confidence badge
         - Confidence styling: HIGH = green badge, MEDIUM = default badge, LOW = yellow/amber border + background per CONTEXT.md decision
         - Content area with inline-editable fields (use contentEditable or controlled inputs)
       - "Accept Import" and "Cancel" buttons at bottom
       - Scrollable container for many sections
       - When user clicks "Accept Import", collect only checked sections and call onAccept
       - For inline editing: use controlled textarea/input fields within each section card. When user types, update the local section content state.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/import-modal.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Dropzone renders with drag-and-drop, text paste textarea works, review panel shows sections with checkboxes and confidence styling.</done>
</task>

<task type="auto">
  <name>Task 2: Build import modal with state machine and wire into editor layout</name>
  <files>src/components/import/import-button.tsx, src/components/import/import-modal.tsx, src/components/editor/editor-layout.tsx, src/__tests__/import-modal.test.tsx</files>
  <action>
    1. Create `src/components/import/import-modal.tsx`:
       - Modal states: 'idle' | 'uploading' | 'parsing' | 'review' | 'confirm-replace'
       - Tab toggle at top: "Upload File" | "Paste Text" (only in idle state)
       - State transitions:
         a. idle -> uploading: file selected in dropzone
         b. uploading -> parsing: PDF sent to /api/parse, response received, now mapping sections
         c. uploading -> parsing (direct): .txt file read client-side via FileReader, text sent to section mapper
         d. uploading -> review (direct): .json file validated against ResumeDataSchema.safeParse, skip section mapping
         e. parsing -> review: sections mapped, display review panel
         f. review -> confirm-replace: user clicks Accept AND existing resume has data
         g. confirm-replace -> done: user confirms replacement
       - File type routing (auto-detect by extension and MIME):
         - .pdf: send to /api/parse, then mapSections on response text
         - .txt: read with FileReader as text, parseText + mapSections client-side
         - .json: read with FileReader as text, JSON.parse + ResumeDataSchema.safeParse. If valid, skip to review with all-HIGH confidence
       - Loading state (per CONTEXT.md): spinner with status text that updates:
         - PDF: "Extracting text..." -> "Mapping sections..." -> "Review ready"
         - Text: "Mapping sections..." -> "Review ready"
         - JSON: "Validating backup..." -> "Review ready"
       - Scanned PDF error: display the error message from API in the modal (don't just toast), with option to switch to "Paste Text" tab
       - Existing data warning (per CONTEXT.md):
         - Check `useResumeStore.getState().resume` — if basics.name is non-empty OR work/education arrays non-empty, show warning
         - Warning text: "You have existing resume data. Importing will replace it."
         - Two buttons: "Continue" and "Cancel"
         - Include "Download backup first" link that calls existing exportJson function
       - On accept: build ResumeData from selected sections, call setResume(), close modal, show success toast

    2. Create `src/components/import/import-button.tsx`:
       - Simple button matching ExportMenu style: outline variant, sm size
       - Upload icon from lucide-react + "Import" text
       - onClick opens ImportModal (manage open state internally or via prop)
       - Import the modal component and render it conditionally

    3. Update `src/components/editor/editor-layout.tsx`:
       - Import ImportButton
       - Add `<ImportButton />` next to `<ExportMenu />` in the desktop header (line ~70)
       - Add `<ImportButton />` next to `<ExportMenu />` in the mobile header (line ~92)
       - Place ImportButton BEFORE ExportMenu for left-right symmetry (Import on left, Export on right) per CONTEXT.md: "Import button placement mirrors Export menu — left-right symmetry"

    4. Write `src/__tests__/import-modal.test.tsx`:
       - Test modal opens when import button clicked
       - Test dropzone renders in idle state
       - Test tab toggle between Upload File and Paste Text
       - Test file type validation (reject files > 5MB)
       - Test JSON import: valid JSON loads directly with success toast
       - Test review panel: sections render with checkboxes
       - Test section deselection: unchecked sections excluded from import
       - Test existing data warning appears when resume has data
       - Test cancel closes modal without changes
       - Mock fetch for /api/parse calls
       - Mock useResumeStore for setResume verification
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/import-modal.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Import button visible in header. Modal opens with dropzone + text paste tabs. File upload routes to correct handler. Review panel shows parsed sections. User can select/deselect/edit sections. Existing data warning with backup link works. Accept commits to store. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete resume import flow: PDF upload, text paste, JSON import, section review with confidence highlighting, inline editing, data replacement warning</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Verify Import button visible next to Export in header
    3. Click Import — modal opens with dropzone and "Upload File"/"Paste Text" tabs
    4. Test PDF upload: drag or select a PDF resume file
       - Should see "Extracting text..." spinner
       - Should see review panel with parsed sections
       - Sections should have confidence badges (some yellow/amber for uncertain)
    5. Test text paste: switch to "Paste Text" tab, paste resume text, click "Import Text"
       - Should see sections mapped from text
    6. Test JSON import: export a JSON backup first, then import it
       - Should load directly without section review
    7. In review panel: uncheck a section, edit a field, click Accept Import
       - Unchecked sections should NOT appear in editor
       - Edited fields should reflect changes
    8. If you had existing data, verify the replacement warning appeared with backup link
    9. After accepting: editor should show imported data, success toast shown
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — all existing + new tests pass
- Import button visible in both desktop and mobile headers
- PDF upload -> parse -> review flow works end-to-end
- Text paste -> parse -> review flow works
- JSON import validates and loads directly
- Scanned PDF shows friendly error message
- Review panel has checkboxes, confidence badges, inline editing
- Existing data warning with backup download link
- setResume() called with correct data on accept
- No regressions in existing export, template, or editor functionality
</verification>

<success_criteria>
- Import button symmetrically placed opposite Export menu
- All three file types (.pdf, .txt, .json) handled with correct routing
- Loading states show progress text per CONTEXT.md decisions
- Review panel allows selection, deselection, and inline editing
- Uncertain fields have yellow/amber visual treatment
- Existing data replacement includes warning + backup option
- Success toast on completed import
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-import/04-02-SUMMARY.md`
</output>
